<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>dwyl | Library</title>
    <link rel="stylesheet" href="https://unpkg.com/tachyons@4.6.1/css/tachyons.min.css"/>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body class="bg-light-grey bg-white-l mw8 center shadow-2-ns">
    <div class="h3 pb1 pt2"></div>
    <nav class="h3 pb1 pt2 fixed w-100 mw8 center right-0 left-0 bg-dwyl-teal top-0">
      <div class="mw8 center relative">
        <%= img_tag(static_path(@conn, "/images/dwyl-heart-only-logo-white.png"), class: "w3-5 mt1 db center") %>
        <div class="right-0 top-0 absolute pt1 show-l">
          <%= link("Search", to: page_path(@conn, :index), class: "link db white fr f5 mr3 mt1 mb1") %>
          <%= link("Feedback", to: page_path(@conn, :index), class: "link db white right f5 mr3") %>
        </div>
      </div>
    </nav>

    <main role="main" class="mw8 center bg-light-grey">
      <%= render @view_module, @view_template, assigns %>
    </main>

    <div role="alert"><%= get_flash(@conn, :info) %></div>
    <div role="alert"><%= get_flash(@conn, :error) %></div>

    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
